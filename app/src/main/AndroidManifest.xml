<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ado.edu.itlas.taskapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".vista.CategoriaListActivity" />
        <activity android:name=".vista.CategoriaActivity" />
        <activity android:name=".vista.LoginRegistroActivity" />
        <activity android:name=".vista.MainActivity" />
        <activity android:name=".vista.UsuarioLista" />
        <activity android:name=".vista.TareaUsuarioTecnico" />
        <activity android:name=".vista.ActivityListaAdapterTecnico" />
        <activity android:name=".vista.TareaUsuarioNormal" />
        <activity android:name=".vista.CrearTareas" />
        <activity android:name=".vista.MostrarTareaCreada" />
        <activity android:name=".vista.UsuarioListaAdapter"></activity>
    </application>

</manifest>